# HAProxy resource ceiling for df-bare (docker HAPROXY_MEM/HAPROXY_CPUS 1:1).
# Drop-in for the distro haproxy.service; CPUQuota is normalized to % by
# init.sh from the profile.
[Service]
MemoryMax={{ .HAPROXY_MEM }}
CPUQuota=25%
